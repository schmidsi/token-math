{
  "description": "NOTE: This is just a development helper. The real package.json is in src/package.json",
  "workspaces": [
    "src"
  ],
  "private": true,
  "version": "0.0.7",
  "license": "MIT",
  "scripts": {
    "deploy": "yarn prepare && cd dist && yarn publish",
    "prepare": "yarn transpile && yarn syncPkg && yarn test:dist",
    "syncPkg": "ts-node scripts/syncPkg",
    "test:dist": "jest dist",
    "test": "jest src",
    "transpile": "yarn rimraf dist && tsc"
  },
  "engines": {
    "node": "^10.4.0"
  },
  "devDependencies": {
    "@types/jest": "^23.3.1",
    "@types/node": "^10.12.0",
    "jest": "^23.5.0",
    "mkdirp": "^0.5.1",
    "ramda": "^0.25.0",
    "rimraf": "^2.6.2",
    "ts-jest": "^23.1.3",
    "ts-node": "^7.0.1",
    "typescript": "^3.0.1"
  }
}
